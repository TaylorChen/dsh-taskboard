# dsh-taskboard V0.4 实施计划

> 状态：待实施 · 基线 `v0.3.0` (`dd93023`) · 目标 `0.4.0`
> 本文是实施依据。每项都写了「为什么做」「改哪里」「怎么算完成」「风险」，不是功能许愿单。
> 两条 seam 的调研均已完成：W1 用 `ctx.workspaceRegistry`（web-only，需可选注入），W2 用 `ctx.subagents.start()` + `run.result`（dsh-base 自带）。

---

## 一、背景

v0.3 让板子能「把工作交给空闲的 agent」（自动认领 + followup 派发）。但它有两个明确的断层，都来自 ARCHITECTURE「Not yet built」：

1. **workspace 是字段，不是事实。** `Task.workspaceId` 从 v0.1 就落盘、可过滤，但没有任何代码解析它或从会话分配它。自动认领也因此**不按工作区过滤**——一个会话可能领到别的工作区的任务。这是「组织层」语义的漏洞：板子声称按工作区分组，却从不兑现。
2. **认领和执行为两段。** `task_claim` / 自动认领只把任务标成 `in_progress` 并派发给**当前**会话；没有把任务交给**后台 subagent** 独立执行的路径。v0.3 的 followup 是在当前会话里继续跑——占用主会话上下文、无法并行多个任务。

v0.4 把这两个断层补上：**workspace 绑定**让「任务属于哪个工作区」成为可解析、可自动分配、可过滤的事实；**subagent 认领执行**让「认领」真正变成「起一个后台执行」。

### 与 v0.2/v0.3 的关系

- v0.2 状态机是语义前提（`open`/`in_progress`/`awaiting_human`/`done` 的流转在这里被复用）
- v0.3 自动认领是执行入口（W1 让它的扫描按工作区过滤；W2 让它的派发目标是 subagent 而非主会话）
- 本文不碰审批门、短 ID、活动流的结构——都是加法

---

## 二、范围

### 做

| 编号 | 工作项 | 破坏性 | 依赖 |
|---|---|---|---|
| **W1** | Workspace 绑定：解析 / 自动分配 / 过滤 | 否 | — |
| **W2** | 认领 → 后台 subagent 执行 | 否 | W1（派发时继承工作区） |

### 不做（附理由）

| 不做 | 理由 |
|---|---|
| 工作区 UI（重命名/排序/归档面板） | `ctx.workspaceRegistry` 已有完整实体 API 与自己的 UI；本插件只消费 id，不复制管理界面 |
| 多工作区并行看板 | 视图数量不是优点；一个视图做深（沿用 v0.2 结论） |
| 任务 ⇄ 子任务（subagent 产生的子任务） | 等 W2 落地、看真实执行模式后再评估 |
| 手动「派给指定 subagent」的工具参数 | v0.4 只做「认领即起 subagent」的自动化路径，手动指定属于工具面扩展 |

---

## 三、工作项详述

### W1 · Workspace 绑定

**动机。** `workspaceId` 是「组织字段」不是「事实字段」。三个具体断层：
- `GET /board` 返回原始 id，面板不显示工作区名（`projectName` 有，workspace 没有）
- `create` / `task_claim` / 自动认领都不写 `workspaceId`（模型建任务默认 `null` = 板级全局）
- v0.3 自动认领扫描**全部** `open` 任务，无视工作区——一个会话可能领到别的项目目录下的任务

**seam 调研结论（`@deepseek-ai/dsh-workspace`，lib/types/index.d.ts）。**

- 服务名 `ctx.workspaceRegistry`，实体 `Workspace { id: WorkspaceId(生成 uuid), path: 规范化目录, title, sessionIds }`
- 会话 ⇄ 工作区映射是 **cwd 相等**：`Workspace.sessionIds` = 规范化 cwd 等于 `workspace.path` 的会话；`resolveByPath(path)` 按目录反查归属
- 会话的 cwd 在 `agent.session.header.cwd`（dsh-session `SessionHeader`）
- **关键约束：`workspaceRegistry` 只存在于 web profile**（`dsh-web-app` 依赖它；`dsh-base`/`dsh-headless` 都没有）——所以本插件**不能**在主 row 的 `inject` 里加 `workspaceRegistry`（会把 headless 启动搞挂），必须用可选注入（`ctx.inject` 子纤程，同 `commands` 的模式）

**改动点。**

- `src/domain.ts` — 无 schema 变化（`workspaceId` 已存在）
- `src/service.ts` — `CreateTaskInput` 增加 `workspaceId`（已有）；新增只读方法 `workspaceOf(ref)`（解析任务的工作区实体信息，未绑定时 `undefined`）
- 可选注入解析器（新文件 `src/workspace.ts` 或并入 index）：`ctx.inject(['workspaceRegistry'], scoped => ...)` 存在时注册「会话 → workspaceId」解析与「id → 展示名」解析
- 自动分配：`task_claim` 与 `task_create` 在注入可用时，若任务 `workspaceId === null` 且能解析出会话所属工作区（`session.header.cwd` → `resolveByPath` → 归属工作区；或遍历 `list()` 找 `sessionIds` 含本会话），则写入该 id
- 自动认领（`src/autoclaim.ts`）：驱动可解析会话工作区时，只扫描**该工作区**（或未绑定）的 `open` 任务；无法解析时维持现状（全板扫描）
- 面板 `TaskboardView.tsx`：卡片 meta 行显示工作区名（解析失败回退为不显示）

**验收标准**

- [ ] 模型用 `task_claim` 认领时，任务自动带上会话所属工作区的 `workspaceId`（注入可用时）
- [ ] 自动认领在会话有工作区时，只领该工作区（或未绑定）的 `open` 任务
- [ ] 面板卡片显示工作区名（有绑定且注入可用时）
- [ ] **headless profile 无 `workspaceRegistry` 时插件照常启动**（可选注入不破坏任何现有行为）
- [ ] `workspaceId` 为 `null` 的旧任务不受影响

### W2 · 认领 → 后台 subagent 执行

**动机。** v0.3 的「认领 = 当前会话 followup」把执行绑死在主会话上：主会话上下文被任务内容占用、多任务无法并行、认领后主会话的下一步必须等任务完成。Codex 的对照调研里「任务是一次执行的入口」还差最后一环——**认领本身就应该能起一个独立的执行者**。

**seam 调研结论（`@deepseek-ai/dsh-subagent`，lib/types/types.d.ts + continuation.d.ts）。**

- `ctx.subagents.start(name, request)` → `Promise<SubagentRun>`，其中：
  - `request.prompt: ContentBlock[]` — 子代理的 user 消息（任务内容就是 prompt）
  - `request.parent: Agent` — 派生的 agent；**工作区/lineage 由它的会话派生**（W2 依赖 W1 的动机在此：任务知道自己的工作区，子代理继承执行环境）
  - `request.signal: AbortSignal` — 取消通道
  - `request.agentOptions?` — 可选模型覆盖
- `SubagentRun`：`id`（子会话 id）、`localAgent`、**`result: Promise<SubagentResult>`**（终结时 resolve，`stopReason: 'completed' | 'error' | …`；基础设施故障才 reject）、`dispose()`
- **`ctx.subagents` 在 `dsh-base` 里，web 与 headless 都有**——不像 workspaceRegistry 有 profile 差异
- `ctx.jobs`（`dsh-jobs` JobRegistry）是观察层：记录 + `JobDoneListener`/`JobsChangedListener`。W2 的核心完成信号用 `run.result` 就够；jobs 留给面板「执行中」指示（可选，不进 v0.4 验收）

**改动点。**

- 服务层新增写路径 `dispatch(ref, sessionId, actor, dispatch)`：把已认领任务交给后台 subagent（prompt = 任务 key/title/body + 「完成后回写」说明），任务保持 `in_progress`，活动流记 `dispatch` 类条目（`from: null, to: subagent 会话 id`）
- 完成回写：`run.result` settle 后，按 `stopReason` 迁移任务（`completed` → `awaiting_human` 等你确认；`error` → `blocked` + reason），活动流记录完成；回写前用 `expectedRevision` 防与人工操作竞争
- 自动认领（v0.3）的派发目标改为 subagent：`ctx.subagents.start` 成功即认领完成；`ctx.subagents` 不可用（理论上不会，但防御）回退 v0.3 的 `agent.followup` 路径
- 并发防重：同一任务已处于「派发中」时（可用活动流的最后一条 `dispatch` 未配对完成，或任务级标记）拒绝二次派发

**验收标准**

- [ ] 认领后任务被后台 subagent 实际执行（子会话存在、有真实日志）
- [ ] subagent `completed` → 任务迁移 `awaiting_human`；`error` → `blocked` + reason
- [ ] 活动流记录 `dispatch` 与完成
- [ ] 任务已在派发中时拒绝二次派发（不起两个 subagent）
- [ ] 无 `ctx.subagents` 的环境回退到 v0.3 followup 路径（防御分支，单测覆盖）

---

## 四、实施顺序

```
W1 workspace 绑定 ──┬─→ W2 subagent 派发（继承工作区）
                    └─→ 自动认领按工作区过滤（W1 内部）
```

1. **W1** —— 独立、无 seam 风险（workspaceRegistry 只读消费）
2. **W2** —— 依赖 W1 的「任务知道自己在哪个工作区」，subagent 才能继承

---

## 五、破坏性变更与回滚

| 变更 | 破坏性 | 处理 |
|---|---|---|
| 无 schema 变化 | 否 | 全部为加法：新方法、新注入、新派发路径 |
| 自动认领默认行为微变 | 否 | 有工作区时只扫本工作区——对单工作区部署无感 |

**回滚**：`git reset --hard v0.3.0`。无迁移负担。

---

## 六、验证清单

**代码层**
- [ ] `pnpm run check` / `build` 全绿
- [ ] 新增测试：workspace 解析/自动分配/过滤、派发与完成回写、并发防重

**集成层**
- [ ] web profile 启动零警告（含 workspaceRegistry 注入）
- [ ] **headless profile 启动零警告（无 workspaceRegistry 的回归）**
- [ ] 真实 dsh 实例：认领后任务带 workspaceId、subagent 执行有真实日志

**行为层**
- [ ] 自动认领在工作区存在时只领本工作区任务
- [ ] 并发认领一个任务只起一个 subagent

---

## 七、风险登记

| 风险 | 影响 | 缓解 |
|---|---|---|
| `ctx.subagents`/`ctx.jobs` 的公开 API 与实现细节有落差 | 中 | W2 开工前用真 API 做 30 分钟 Spike（同 S1 模式），结论记入 ARCHITECTURE |
| 会话 cwd 可能不属于任何已登记工作区 | 低 | `resolveByPath` 返回 `undefined` 时不分配、不过滤（保持现状） |
| subagent 完成回写与人工操作竞争 | 低 | 复用 `expectedRevision` 乐观并发 |
| 起 subagent 有环境限制（spawn/worker） | 中 | 回退 v0.3 followup 路径；不可用不算故障 |

---

## 八、完成定义

1. W1/W2 验收标准全部勾选
2. 第六节验证清单全部通过（含 headless 回归）
3. ARCHITECTURE.md 补齐：workspace 绑定决策、W2 seam 调研结论、回退策略
4. README 更新：工作区语义、subagent 执行说明
5. SECURITY.md 复核：subagent 执行引入的模型可见文本与自动化写
6. CHANGELOG 记录；版本 `0.4.0`
