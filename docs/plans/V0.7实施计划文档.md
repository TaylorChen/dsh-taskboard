# dsh-taskboard V0.7 实施计划

> 状态：待实施 · 基线 `v0.6.0` (`6d5a6b4`) · 目标 `0.7.0` · ROADMAP **L4 依赖与调度**
> 本文是实施依据。

---

## 一、背景（第一性原理）

v0.3 的额度门槛回答了「上下文满了别领」，v0.5/0.6 让任务可执行且带证据。但
「该领哪个」仍是最原始的：按创建时间。Agent 可以并行执行，代价是上下文成本与
顺序错误——**调度是 agent 时代唯一能控制成本与顺序的手段**（ROADMAP L4）。
v0.2 曾排除父子/阻塞依赖，理由是状态机未稳；现在状态机、自动认领、subagent 派发
全部就位，依赖的价值（自动编排）远高于人类模式（只是可视化）。

## 二、范围

### 做

| 编号 | 工作项 | 破坏性 | 依赖 |
|---|---|---|---|
| **W1** | `dependsOn` 前置任务 + 无环校验 | 否（加法） | — |
| **W2** | 就绪判定 + 加权候选（依赖就绪 × 优先级 × 等待） | 否 | W1 |
| **W3** | 执行预算：任务级 `budgetTokens`，派发 `maxTokens`，超支自动受阻 | 否 | — |

### 不做

| 不做 | 理由 |
|---|---|
| 完整 DAG 可视化/依赖图 UI | 面板只做「前置未完成」提示；图属于视图深度问题 |
| 队列系统/分布式调度 | 单进程插件，只需加权候选，不做队列抽象 |
| 自动拆分子任务 | v0.8 后评估 |

## 三、工作项详述

### W1 · dependsOn + 无环校验

**改动点。**

- `src/domain.ts` — `taskSchema` 增加 `dependsOn: z.array(z.string().min(1)).max(32).default([])`
  （存任务 id；加法字段，老记录默认 `[]`）
- `src/service.ts`：
  - `CreateTaskInput` / `UpdateTaskInput` 增加 `dependsOn?: readonly string[]`（update 整体替换）
  - 写入时校验：每个依赖可解析（key 或 id）且**不成环**（沿 dependsOn 递归，回到自身
    即环 → `invalid-input`）
  - 依赖被删除/缺失 → 写入拒绝（`invalid-input`）还是容忍？——容忍（删除任务不该
    卡死依赖者）：缺失依赖视为「未就绪」而非错误。写入时只校验存在的依赖无环。

**验收**

- [ ] `dependsOn` 落盘、老记录默认 `[]`
- [ ] 依赖自身 → 环 → `invalid-input`
- [ ] A→B→A → 环 → `invalid-input`
- [ ] 依赖不存在 → 写入成功（就绪判定时视为未就绪）

### W2 · 就绪判定 + 加权候选

**改动点（`src/service.ts` + `src/autoclaim.ts`）。**

- 服务层 `isReady(ref)`：任务的每个依赖状态 ∈ `{done, cancelled}`（缺失依赖 = 未就绪）
- `src/autoclaim.ts` `selectClaimCandidate` 改为接受就绪过滤 + 加权排序：
  - 过滤：`open` + 未认领 + **依赖全部就绪**
  - 排序：优先级权重（urgent 4 … low 1）降序，其次 `createdAt` 升序
- 面板：卡片显示「前置未完成」提示（依赖未就绪时）

**验收**

- [ ] 依赖未完成的 open 任务不被自动认领
- [ ] 依赖完成后（done）可被认领
- [ ] 高优先级先被认领（同就绪条件下）
- [ ] `selectClaimCandidate` 纯函数单测

### W3 · 执行预算

**改动点。**

- `src/domain.ts` — `budgetTokens: z.number().int().nonnegative().nullable().default(null)`
  （任务级输出预算，`null` = 不限制）
- `src/autoclaim.ts` — 派发时若任务有 `budgetTokens`，传
  `agentOptions: { maxTokens: budgetTokens }`（子代理输出上限）
- `src/service.ts` — `settleDispatch` 识别 `stopReason: 'max-tokens'` → 结算
  `blocked` + 诊断「预算超支（maxTokens）」——在 autoclaim 的 result handler 里
  处理（stopReason === 'max-tokens' → error 结算，reason 带预算超支）

**验收**

- [ ] 有预算的任务派发时带 `maxTokens`
- [ ] `max-tokens` 结算 → blocked + 预算超支诊断
- [ ] 无预算任务行为不变

## 四、实施顺序

W1 → W2 → W3。

## 五、破坏性变更与回滚

全部加法字段；`git reset --hard v0.6.0` 可回滚。

## 六、验证清单

**代码层**：`pnpm run check` / `build` 全绿；单测覆盖无环/就绪/加权/预算。
**集成层**：真实 E2E——A 依赖 B：B 未完成时 A 不可认领；B 完成后 A 被自动认领。

## 七、风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 依赖删除导致永久未就绪 | 中 | 面板提示缺失依赖；手动解除 dependsOn 可恢复 |
| 加权排序过度设计 | 低 | 只做优先级 + 等待时长，不引入多目标优化 |
| maxTokens 语义与 provider 不符 | 低 | `max-tokens` stop reason 兜底；无预算时行为不变 |

## 八、完成定义

1. W1–W3 验收勾选；2. 验证清单通过；3. 文档（ARCHITECTURE 34 / README / CHANGELOG）；
4. 版本 `0.7.0` 提交 + tag。
