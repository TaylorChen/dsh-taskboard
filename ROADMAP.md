# dsh-taskboard ROADMAP — Agent 模式下的任务管理

> 从第一性原理出发的迭代路线图。任务管理在无 AI 时代就存在（Jira / 云效 / TAPD），
> 本项目的完成度取决于我们如何看待「软件开发中的任务管理」。本文先给出本质分析，
> 再给出分阶段迭代计划。每个迭代都回答同一个问题：**这个能力是 agent 时代独有的
> 高价值，还是传统功能的移植？** 只做前者。

---

## 一、第一性原理：任务管理在协调什么

传统任务管理协调的是**稀缺的人**：人的时间、注意力、排期稀缺，所以工具重流程
（审批、流转规则）、排期（估时、甘特图）、权限（谁能改）、报表（度量人效）。
状态机的意义是「任务在不同人之间交接时的可见性」。

Agent 时代，稀缺的东西变了：

| 稀缺物 | 人类模式 | Agent 模式 |
|---|---|---|
| 执行能力 | 人的时间（贵，需排期） | agent 起量便宜，**token 贵** |
| 上下文 | 人脑自带团队默契 | **任务必须自包含**（agent 无默契、无记忆） |
| 信任 | 人做错可追问 | **agent 会幻觉**，完成不等于完成 |

三个基本面变化由此推出：

1. **任务的本质从「描述」变成「可执行规格」**——任务板的第一职责从「追踪进度」
   变成「把人的意图翻译成自包含规格，把 agent 的产出翻译回可验证结果」。
2. **协调从「谁有空」变成「额度调度」**——谁先做、做多少、能否并行，由上下文
   成本与依赖决定，不由人盯。
3. **历史从「审计档案」变成「活的知识」**——每个任务是一次执行的完整轨迹，
   应回流成下一个任务与下一个会话的上下文。

## 二、五层能力模型

| 层 | 本质 | 状态 |
|---|---|---|
| **L1 执行层** | 任务在人与 agent 间流转的机制：状态机、审批门、标识、活动流、认领、派发、工作区 | ✅ v0.1–v0.4 |
| **L2 规格层** | 任务自包含：验收标准、上下文引用、完成定义（DoD） | ⬜ v0.5 |
| **L3 闭环层** | 完成 = 有证据的完成：自动验证、证据归档、失败诊断 | ⬜ v0.6 |
| **L4 调度层** | 依赖 + 额度 + 优先级驱动的自动执行编排 | ⬜ v0.7 |
| **L5 知识层** | 历史任务回流成新任务/新会话的上下文 | ⬜ v0.8 |

**排除清单（第一性原理排除，永不进入路线图）：** 甘特图/估时（agent 执行时长
不可估，v0.2 已论证）；工时报表（agent 时代的度量是 token 成本与成功率，不是工时，
且是 L8 之后的事）；复杂权限/审批流（单用户本地工具，权限是伪需求）；重复任务
（走 `ctx.schedule`，不混进领域模型）。

---

## 三、迭代计划

### V0.5 · 任务规格化（L2 规格层）

**为什么。** 认领与派发（L1）跑通后暴露的瓶颈是：**agent 领了任务不知道怎么做**。
"修复登录 bug" 对人足够，对 agent 不够——它不知道代码在哪、成功长什么样。任务板
必须把「意图」升级成「规格」。

**改动点。**
- schema 加法：`spec` 块 —— `acceptanceCriteria: string[]`（可验证的验收标准）、
  `contextRefs: string[]`（文件/commit/issue 引用）、`definitionOfDone: string`（可选）
- **`draft` 语义化**：draft = 规格不完整。认领/派发前做规格自包含检查（启发式：
  有验收标准、有上下文引用、标题动词开头）；不完整的任务留在 `draft`，不能被认领
- 工具：`task_create` / `task_update` 支持规格字段；面板 draft 列显示「缺验收标准」
  等完整性提示
- `task_create` 由模型建的任务缺规格 → 自动落 `draft` 而非 `open`

**验收方向。** 缺规格任务无法进入 `open`；认领到的任务必有验收标准；模型建任务
自动进 `draft` 并被提示补规格。

**风险。** 规格字段可能被滥用为「填了但没用」——缓解：spec 检查只做启发式，
不做格式教条；面板给出缺什么的具体提示。

---

### V0.6 · 验证闭环（L3 闭环层）

**为什么。** 信任问题：**agent 说完成了不等于完成了**。L1 的 `completed→awaiting_human`
让人重查——但人的时间应该花在确认结论，不是重跑执行。闭环层把「完成」变成
「有证据的完成」。

**改动点。**
- **DoD 证据收集**：subagent 完成时自动收集证据（测试输出、文件 diff、commit hash、
  产物路径），结构化存进任务（`evidence` 字段或 activity 扩展）
- **验收标准自评**：派发 prompt 要求子代理逐条对照 `acceptanceCriteria` 自评并引用
  证据；回写时带自评结果
- **失败诊断**：`error` 结算时自动 attach 失败摘要（stop reason、最后一步输出），
  `blocked` 列直接显示诊断而非裸 reason
- 面板：`awaiting_human` 卡片显示证据清单，人只需确认或打回（打回 = 一条带原因的
  `status` 迁移，规格层决定它回 `draft` 还是 `open`）

**验收方向。** 结算到 `awaiting_human` 的任务必有证据；`blocked` 带诊断；打回路径
有明确落点。

**风险。** 证据收集可能把 token 花在「写报告」上——缓解：证据来自工具结果的
结构性截取，不要求子代理额外长篇叙述。

---

### V0.7 · 依赖与调度（L4 调度层）

**为什么。** v0.3 的额度门槛回答了「上下文满了别领」，但没回答「该领哪个」。
Agent 可以并行执行，代价是上下文成本与顺序错误——**调度是 agent 时代唯一能控制
成本与顺序的手段**。v0.2 曾明确「不做父子/阻塞依赖」——当时的理由是状态机未稳；
现在状态机、自动认领、subagent 派发都就位，依赖的价值（自动编排）远高于人类模式
（只是可视化）。

**改动点。**
- schema 加法：`dependsOn: string[]`（前置任务 id，无环校验）
- **就绪判定**：`open` 列拆「依赖已就绪」才可认领；自动认领扫描就绪任务
- **调度权重**：认领候选 = 依赖就绪 × 优先级 × 额度剩余 × 等待时长（简单的
  加权，不做复杂算法）
- **执行预算**：每任务 token 预算上限（`Config` 或任务级覆盖），超预算自动
  `blocked` + 「预算超支」诊断，而不是无限烧 token

**验收方向。** 依赖未就绪的任务不可认领；循环依赖被拒；超预算任务自动受阻；
多任务按权重序执行。

**风险。** 调度逻辑过度设计——缓解：v0.7 只做「依赖 + 加权候选」，不引入队列
系统、不引入分布式概念。

---

### V0.8 · 经验回流（L5 知识层）

**为什么。** 这是 agent 任务管理**最大的增量价值**：Jira 的历史是死档案，agent
任务板的历史是活上下文。每个完成的任务 = 一次执行的知识（问题、方案、证据、
结论），流回下一个任务能直接降低上下文成本——新任务不用从零探索。

**改动点。**
- **经验卡**：`done` 任务自动归档为结构化摘要（`outcome` 字段：做了什么、产物在哪、
  结论/教训），由结算时的证据派生，不需要额外一轮模型
- **创建时注入**：新任务创建时，按 workspace + 标签检索相关历史经验卡，注入
  `contextRefs` 参考（模型可见，降低重复探索）
- **会话组织层**：新会话启动时，任务板把「相关未完成任务 + 相关历史结论」作为
  会话初始上下文注入——任务板真正成为会话的组织层（v0.2 计划里的「任务板是会话的
  组织层」至此闭环）

**验收方向。** 完成任务的结论可在后续任务创建时被引用；新会话能看到相关历史而不
需要用户转述。

**风险。** 注入过多上下文反而浪费 token——缓解：检索只取 top-K 且限制字数；
默认关闭注入（opt-in）。

---

## 四、实施节奏

```
V0.5 规格化 ──→ V0.6 验证闭环 ──→ V0.7 调度 ──→ V0.8 知识层
   （L2）          （L3）            （L4）         （L5）
```

- 每个版本沿用既有流程：先写实施计划文档 → 确认 → 实施 → 单测 + 真实 E2E → 文档
- V0.5/V0.6 依赖已就绪，可直接开工；V0.7 依赖 V0.5（就绪判定需要规格完备性）；
  V0.8 依赖 V0.6（经验卡需要证据）
- 每版都保持「可回滚、无迁移负担」：规格/依赖/证据都是加法字段

## 五、本路线图的第一性原理检验

每层回答「降低什么成本」：

| 层 | 降低的成本 | 传统工具的对应物 | 为何不是移植 |
|---|---|---|---|
| L1 执行层 | 人与 agent 的协调成本 | 状态机/流程 | 审批门按发起方区分，agent 写需人批 |
| L2 规格层 | agent 的探索成本（token） | 需求文档 | 需求文档给人读，规格给 agent 执行且可检查 |
| L3 闭环层 | 人的验证成本（信任） | 测试报告/验收流程 | 证据自动收集并回写，人是确认者不是重跑者 |
| L4 调度层 | 上下文成本与顺序错误 | 排期/甘特图 | 按额度与依赖自动编排，不是按人排期 |
| L5 知识层 | 重复探索成本 | 知识库/Wiki | 经验从执行自动派生，注入到执行点 |

**结论**：dsh-taskboard 的护城河不是「又一个看板」，而是把任务管理从
「追踪人的工作」改造成「组织 agent 的执行」——规格、证据、调度、知识四条线，
每条都是传统工具在 agent 时代失效后需要重做的部分。v0.1–v0.4 是 L1 执行层，
v0.5 起进入 agent 独有的价值区。
