# dsh-taskboard V0.2 实施计划

> 状态：待实施 · 基线 `v0.1.0` (`ff7ad62`) · 目标 `0.2.0`
> 本文是实施依据。每项都写了「为什么做」「改哪里」「怎么算完成」「风险」，不是功能许愿单。

---

## 一、背景

v0.1 已经跑通并在真实 dsh `0.1.0-rc.6` 上端到端验证过：跨会话持久、审批门、乐观并发、面板读写。

v0.2 的输入来自一次对照调研——体验了 Codex 内置的「任务面板」（完整 issue tracker：仪表盘/看板/列表/甘特图 + 自动认领）。**结论不是抄它的功能表**，而是提炼出五条我们缺失的设计思想。

### 借鉴的五条设计思想

| # | 思想 | 我们 v0.1 的问题 |
|---|---|---|
| 1 | **状态机围绕「球在谁手上」，不是「工作阶段」** | 状态抄自人类 issue tracker，无法驱动通知、认领、告警 |
| 2 | **标识符要能在人、模型、外部系统之间流转** | UUID 模型念不动、人说不出口，已因此踩过一次坑 |
| 3 | **任务是「一次执行的入口」，任务板是会话的组织层** | `claimedBySessionId` 只当元数据，没做成产品 |
| 4 | **活动历史里 agent 和人是对等主体** | 只有 `revision`/`updatedAt`，看不到谁在何时改了什么 |
| 5 | **自动化绑在额度上，不是绑在开关上** | 无自动化（v0.3 再做，本文只留接口余量） |

### v0.2 与 v0.3 的分界

v0.2 做 **1–4**（地基）。**5（自动认领）放 v0.3**，因为它依赖前四条全部就位，且需要先摸清 `ctx.tokenMeter` 能提供什么额度信号。

---

## 二、范围

### 做

| 编号 | 工作项 | 破坏性 | 依赖 |
|---|---|---|---|
| **W1** | 状态机重做（7 态） | ⚠️ 是 | — |
| **W2** | 人类可读短 ID（`TB-1`） | ⚠️ 是 | — |
| **W3** | 活动流（who / when / what） | 加表 | W1 |
| **W4** | 任务 ⇄ 会话绑定与跳转 | 否 | W1 |
| **W5** | 每列独立「+」按钮 | 否 | W1 |

### 不做（附理由）

| 不做 | 理由 |
|---|---|
| 甘特图 | 前提是「工期可估」。agent 任务时长不可预测（同一任务 2 分钟或 40 分钟取决于模型与上下文），甘特图在这里是错误隐喻 |
| 列表视图 / 仪表盘 | 视图数量是产品成熟度的体现，不是设计优点。插件应把一个视图做深 |
| 重复任务属性 | 周期性工作应走 `ctx.schedule`（dsh 已有该 seam），做成任务字段是把调度混进领域模型 |
| 附件 | dsh 有 `ctx.attachments` seam，但这是低频需求，等有人提再说 |
| 父子议题 / 阻塞依赖 | 有价值但不紧急；等状态机稳定后再评估，避免一次改动面过大 |

---

## 三、工作项详述

### W1 · 状态机重做

**动机（思想 1）。** 现有 `todo / in_progress / in_review / done / cancelled` 描述「工作进行到哪一步」。人机协作的看板需要描述「现在轮到谁动」——这直接决定三件下游能力：通知发给谁、面板高亮什么、自动认领扫哪一列。

**新状态集**（代码用英文，中文在 `src/client/locales.ts`）：

| 代码值 | 中文 | 语义 | 球在谁 |
|---|---|---|---|
| `draft` | 待立项 | 还没想清楚，等人定义 | 人 |
| `open` | 等待认领 | 定义好了，可被 agent 领走 | agent（可领） |
| `in_progress` | 处理中 | agent 手上 | agent |
| `awaiting_human` | 等你确认 | 球回到人手上 | 人 |
| `blocked` | 遇到阻碍 | agent 卡住，需要人来解 | 人（异常） |
| `done` | 完成 | — | — |
| `cancelled` | 取消 | — | — |

`blocked` 是 v0.1 完全缺失的一态。agent 长时间运行卡住时，必须能显式表达，否则卡住的任务和进行中的任务混在一起。

**改动点**

- `src/domain.ts` — `TASK_STATUSES` 改写；新增 `blockedReason: z.string().max(2000).nullable().default(null)`
- `src/service.ts` — `update()` 增加约束：迁入 `blocked` 必须带 `blockedReason`；迁出 `blocked` 清空它
- `src/tool.ts` — 新增模型工具 `task_block(id, reason)`，让 agent 自己上报卡住；各工具 `enum` 同步
- `src/client/locales.ts` — 新增 7 条列名
- `src/client/TaskboardView.tsx` — `COLUMNS` 同步；`blocked` 列用告警色；卡片显示 `blockedReason`
- `src/commands.ts` — `/task list` 的状态参数校验同步

**迁移策略（关键）**

状态是**枚举**不是加法字段，老记录的 `todo` / `in_review` 会直接 parse 失败 → `invalid-record`，板子打不开。

采用**读侧别名归一**，不 bump `DOMAIN_VERSION`：

```ts
const LEGACY_STATUS: Record<string, TaskStatus> = {
  todo: 'open',
  in_review: 'awaiting_human',
}

status: z.preprocess(
  value => (typeof value === 'string' && value in LEGACY_STATUS)
    ? LEGACY_STATUS[value]
    : value,
  z.enum(TASK_STATUSES),
)
```

老记录读出来即是新值，下次写入时落盘归一。**不需要用户导出导入**。别名表保留到 v1.0，届时连同这段一起删。

**验收标准**

- [ ] 写入 `status: 'todo'` 的旧 medium 能正常打开，读出为 `open`
- [ ] `update` 迁入 `blocked` 不带 reason → `invalid-input`
- [ ] `task_block` 由模型调用 → 走审批门 → 卡片进「遇到阻碍」列并显示原因
- [ ] 迁出 `blocked` 后 `blockedReason` 为 `null`
- [ ] 面板 7 列在 1280px 宽度下不出现横向滚动（沿用 v0.1 的 `minWidth` 收缩地板策略）

---

### W2 · 人类可读短 ID

**动机（思想 2）。** `TB-1` 能被人口头说出、被模型用 3 个 token 引用、写进 commit message、粘进群聊。UUID 三样都做不到。v0.1 已经因此踩坑一次（`render` 截断 id 导致模型调用不存在的任务）。

**设计**

- 新增字段 `key: string`，形如 `TB-1`，全板唯一、单调递增、**删除后不复用**
- 前缀 `TB` 作为 `Config.keyPrefix` 暴露，默认 `TB`
- `id`（UUID）保留为主键不变，`key` 是展示与引用层
- 所有按 id 查的入口（`task_update` / `task_claim` / `task_block` / `GET|PATCH /task/:id` / `/task show`）**同时接受 `key` 和 `id`**，服务层统一解析
- `output.render` 一律展示 `key`，不再展示 UUID

**计数器存放（需先做技术验证）**

递增计数器需要一个全板范围的槽位。首选**方案 A**：给 domain spec 增加 `global` 槽 `{ nextTaskNumber: number }`。

⚠️ **风险**：v0.1 的 spec 刻意没有 `global`（决策 7）。`descriptorOf()` 会把 `hasGlobal` 投影给后端，**已有 medium 的描述符与新描述符不一致时是否会被判 `malformed-medium`，必须先验证**。

> **Spike S1（≤30 分钟，W2 开工前必做）**：拿一个已有 `taskboard.json`，用带 `global` 槽的 spec 打开，看是否报错。
> - 通过 → 走方案 A
> - 失败 → 走**方案 B**：计数器存进 `projects` 表的一条保留记录（id 固定为 `__meta`），不改描述符。方案 B 更丑但零风险。

**老记录回填**

`key` 无法用 `default` 补（每条要唯一）。在 `apply()` 挂载时做一次性回填：按 `createdAt` 升序给无 `key` 的记录依次编号，然后写回。这是继「播种首个项目」之后第二处不过审批门的写——**属于插件自有的引导逻辑，不是用户或模型发起的写**，需在 ARCHITECTURE 中明确记录。

**验收标准**

- [ ] Spike S1 有明确结论并记录在 ARCHITECTURE
- [ ] 全新板子建的第一条任务 `key === 'TB-1'`
- [ ] 老板子挂载后所有记录都有唯一 `key`，重启不重复回填
- [ ] 删除 `TB-2` 后再建，新任务是 `TB-3` 而非 `TB-2`
- [ ] `task_update` 传 `TB-1` 与传完整 UUID 效果一致
- [ ] `task_list` 的 render 输出里不再出现 UUID

---

### W3 · 活动流

**动机（思想 4）。** Codex 的活动流里「Navidad Kien 将状态从 X 改为 Y」和「Codex Agent 将状态从 X 改为 Y」用同一格式并排——**没有把 agent 特殊化成「系统操作」**。v0.1 的 `origin` 字段只是给记录打标签，没有历史。

**设计**

domain 新增表 `activity`：

```ts
activitySchema = z.object({
  id: z.string(),
  taskId: z.string(),
  at: z.number().int(),
  actor: z.enum(['human', 'agent']),
  actorLabel: z.string(),          // 会话 id 或 'panel' / 'command'
  action: z.enum(['created', 'status', 'edited', 'removed', 'blocked', 'claimed']),
  from: z.string().nullable(),
  to: z.string().nullable(),
})
```

- 服务层每次成功写入后追加一条，**失败不记**（被拒的写不进活动流，审批拒绝本身已落在会话日志的 `approval/asked+decided` 对里）
- 保留策略：`Config.activityRetentionPerTask`，默认每任务 50 条，超出裁剪最旧
- 新增 `GET /api/taskboard/task/:id/activity`
- 面板：卡片点开抽屉展示活动流

⚠️ **同 W2 的风险**：加表会改变描述符的 `tables` 列表。**Spike S1 一并验证加表的兼容性**，两个改动一起决定方案。

**验收标准**

- [ ] 每次 create / status 迁移 / edit / remove 各产生一条活动
- [ ] 被审批拒绝的写不产生活动
- [ ] 人与 agent 的记录格式一致，仅 `actor` 不同
- [ ] 超过保留条数时裁剪最旧，不影响任务本身
- [ ] 面板抽屉能按时间倒序展示

---

### W4 · 任务 ⇄ 会话绑定与跳转

**动机（思想 3）。** agent 时代的任务不是一条记录，是**一次执行的入口**。任务板因此是会话的组织层。v0.1 存了 `claimedBySessionId` 却没有任何入口用它。

**设计**

- 面板卡片：已认领的任务显示会话来源，提供「在对话中打开」
- 服务层新增只读方法 `sessionOf(taskId)`
- `task_claim` 已写入 `claimedBySessionId`，本项只是把它变成产品

> **Spike S2（≤1 小时，W4 开工前必做）**：确认客户端切换会话的官方方式。
> 候选：`ctx.sessions` 运行时 API / 深链参数（生态里 `dsh-deeplink` 用 `?session=`）。
> **禁止**直接操作 DOM 或 history——那会违反「跨插件协作走 cordis 服务」的约束。

**验收标准**

- [ ] Spike S2 结论记录在 ARCHITECTURE
- [ ] 点击跳转能切到正确会话，且不刷新整页
- [ ] 未认领的任务不显示该入口
- [ ] 会话已被删除时显示失效提示而非报错

---

### W5 · 每列独立「+」按钮

**动机。** 建任务时人心里已经知道它属于哪一列。v0.1 只有一个全局「+ 新建任务」，强迫用户建完再改状态。

**改动点**：`TaskboardView.tsx` 列头加 `+`，内联表单带上目标 `status`。纯前端，无 schema 变更。

**验收标准**

- [ ] 在「处理中」列点 `+` 建出的任务直接落在该列
- [ ] 表单在列内展开，不遮挡其它列

---

## 四、实施顺序

```
Spike S1（描述符兼容）──┬─→ W2 短 ID ─┐
                        └─→ W3 活动流 ─┤
W1 状态机重做 ──────────────────────────┼─→ W5 每列 +
                                        │
Spike S2（会话跳转）────→ W4 会话绑定 ──┘
```

1. **Spike S1** —— 它同时决定 W2 和 W3 的方案，必须最先做
2. **W1 状态机** —— 地基，且是 W3/W4 语义的前提；越晚改代价越大
3. **W2 短 ID** —— 独立，收益立竿见影
4. **W3 活动流** —— 依赖 W1 的状态语义
5. **Spike S2 → W4** —— 可与 W3 并行
6. **W5** —— 收尾，纯前端

---

## 五、破坏性变更与回滚

| 变更 | 破坏性 | 处理 |
|---|---|---|
| 状态枚举 | 老值 parse 失败 | 读侧别名归一，**不 bump 版本**，用户无感 |
| 新增 `key` | 老记录无此字段且不能默认 | 挂载时一次性回填 |
| 新增 `activity` 表 / `global` 槽 | 描述符变化 | **Spike S1 决定**；若不兼容走方案 B |
| `blockedReason` | 加法字段 | `.default(null)`，与 `origin` 同模式 |

**回滚**：`git reset --hard v0.1.0`。v0.1.0 是验证过可运行的基线。若已有用户数据被 v0.2 写过（状态归一、`key` 回填），回滚到 v0.1 后这些记录的新状态值会 parse 失败——**因此 v0.2 发布前必须在 README 提示先 `/task export`**。

---

## 六、验证清单

沿用 v0.1 已建立的端到端流程（README「Development」节），**每项工作完成后至少跑一遍相关段落**：

**代码层**
- [ ] `pnpm install --frozen-lockfile` / `typecheck` / `test` / `build` 全绿（pnpm 10 与 11 双版本）
- [ ] 新增测试覆盖：状态迁移约束、别名归一、短 ID 唯一性与回填、活动流写入与裁剪

**集成层**
- [ ] `dsh plugin add` → `--dump-config` 见层
- [ ] 真实 `dsh web` 启动零警告
- [ ] 用 **v0.1 写过的 medium** 启动 v0.2 —— 这是本次最重要的一条
- [ ] 五条路由（含两条写路由）状态码正确；CSRF 415 仍生效
- [ ] 重启幂等：不重复回填 `key`、不重复播种项目

**行为层**
- [ ] 审批拒绝路径：模型写被拒 → 零落盘 → 无活动记录
- [ ] 真实模型轮次：`task_projects → task_create → task_block → task_update`
- [ ] 乐观并发：过期 `expectedRevision` → 409
- [ ] 跨进程：headless 写、web 读

**面板**
- [ ] 7 列在 1280px 不横向滚动
- [ ] 活动流抽屉、会话跳转、每列 `+` 均可用
- [ ] **深色主题上屏验证**（v0.1 遗留未验事项）

---

## 七、风险登记

| 风险 | 影响 | 缓解 |
|---|---|---|
| **描述符变化导致老 medium 打不开** | 高——用户数据打不开 | Spike S1 前置；备用方案 B 零风险 |
| `storage-domain` 设计说明仍在 `proposed/` | 中——上游 API 可能变 | 继续维持「provider 类型只出现在 `src/store.ts`」的隔离 |
| 状态语义过度设计 | 中——7 态对小板子偏重 | `draft` 与 `blocked` 在面板可通过配置隐藏（列表仍保留） |
| 会话跳转无官方 API | 中——W4 可能降级 | Spike S2 结论若为否，降级为「复制会话 id」 |
| v0.2 写过的数据无法回滚到 v0.1 | 中 | 发布前 README 强提示先 export |

---

## 八、完成定义

v0.2 可以打 tag 的条件：

1. W1–W5 的验收标准全部勾选
2. 第六节验证清单全部通过，**含用 v0.1 medium 启动这一条**
3. ARCHITECTURE.md 补齐新决策：状态机语义、别名归一为何不 bump 版本、`key` 回填是第二处非审批写、S1/S2 的结论
4. README 更新：新状态表、短 ID 用法、活动流、升级前先 export 的提示
5. SECURITY.md 复核：活动流是否引入新的模型可见文本（`blockedReason` 会进模型上下文，需说明）
6. CHANGELOG 记录破坏性变更与迁移方式

---

## 附录 · 明确排除的 Codex 特性及理由

| 特性 | 排除理由 |
|---|---|
| 甘特图 | agent 任务工期不可估，隐喻错误 |
| 仪表盘 AI 摘要 | 意图正确（首屏应给判断而非罗列），但不必每次调模型；v0.3 用规则实现 |
| 四个并列视图 | 视图数量是产品成熟度体现，非设计优点 |
| 重复任务 | 应走 `ctx.schedule`，不是任务字段 |
| 25MB 附件 | 低频；真要做走 `ctx.attachments` seam |

**差异化定位**：不追求成为 Linear/Jira 的替代品。本插件的护城河是 **agent-native** ——审批门（Codex 没有）、认领、阻塞回报、结论归档、跨会话，以及作为规范 dsh 插件的可卸载可组合。
