# V1.4 实施计划文档 —— 看板可用性（项目视图 + 看板概览 + 手动排序）

> 本文是实施依据。v1.4 = 可用性：看板会从「一块面板」变成「一片混乱」——项目混在一起、
> 没有一览视角、顺序不可控。三件事必须有解。

## 一、问题

1. **项目混在一起**。任务有 `projectId`，路由也带项目，但面板没有项目维度——所有项目
   的任务挤在七列里，看板一大就分不清谁是谁的。
2. **没有一览视角**。列头计数只报单列；「一共有多少活、多少在等人、多少逾期」要人肉
   加总。等人列(awaiting_human/blocked)的警示色在单列里，缺一个整体信号。
3. **顺序不可控**。列内顺序是存储顺序（updatedAt 倒序）+ 逾期置顶，用户无法把重要的
   卡片钉到前面。

## 二、设计

### E1 · 项目视图（过滤）

**为什么。** 项目是任务的第一归属维度；看板应该能按项目聚焦。

**设计（纯面板 + 已有路由）：**

- 面板头部加项目下拉（`全部` + 每个项目名）；选择后拉
  `GET /board?project=<id>`（路由 v0.2 已支持 `status` 过滤，需补 `project` 参数）。
- 空项目无下拉项时保持「全部」；归档视图同样受项目过滤（组合参数）。

**验收**

- [x] 选择项目后只显示该项目任务；切回「全部」恢复
- [x] 项目过滤与归档视图、列过滤可组合

### E2 · 看板概览统计条

**为什么。** 「还有多少活」应该一眼可见。

**设计（纯面板 + 无 schema）：**

- 头部加一行概览：总任务数、开放（open）、进行中（in_progress）、**等人
  （awaiting_human+blocked，警示色）**、逾期数、已完成（done）。全部由当前 `board`
  载荷派生，无新接口。
- 逾期数 = `dueAt < now` 且未 done/cancelled 的任务数。

**验收**

- [x] 概览条数字与各列一致；等人计数警示色
- [x] 逾期数只统计未完结任务

### E3 · 列内手动排序

**为什么。** 人的优先级 ≠ 时间优先级；卡片应该能被钉住。

**设计：**

- 加法字段 `sortOrder: number | null`（默认 null = 不参与排序，落 updatedAt 之后）。
- `TaskboardService.reorder(refs: TaskId[])`：校验 refs 非空、全部存在、去重后数量与
  传入一致，且**同一列**（status+projectId 一致）——按数组顺序赋 `sortOrder = 0..n-1`，
  该列其余任务 `sortOrder = null`；一次审批（批量治理写，human 动作，无审批——与归档
  一致）。返回受影响任务数。
- `list()` 排序：`sortOrder` 升序优先，null 排最后，再按 updatedAt 倒序。
- 路由 `POST /api/taskboard/reorder`，body `{ refs: [...] }` → `{ reordered: n }`。
- 面板：卡片 `draggable`（HTML5 DnD），列内拖放后 `POST /reorder`（该列当前顺序的
  id 数组），成功后重载；拖拽中高亮目标列。逾期置顶排序在手动排序之后仍是面板侧
  渲染层（手动顺序是存储层事实，逾期浮顶是渲染层提示，二者不冲突——逾期卡片在手动
  顺序内仍然浮到可见顶部）。

**验收**

- [x] 单测：reorder 校验（空/重复/跨列/不存在 id 拒绝）；排序生效；null 复位
- [x] 真实 E2E：POST reorder 后 `/board` 按新顺序返回；恢复默认（全部 null）
- [x] 面板拖拽可重排该列（上屏核对）

## 三、实施顺序

E1 → E2 → E3。（先项目维度，再一览视角，最后顺序控制。）

## 四、破坏性变更与回滚

全部加法：`sortOrder` 字段（`.default(null)`）、新路由、面板增强。
`git reset --hard v1.3.0` 可回滚。

## 五、验证清单

**代码层**：check/build 全绿；E3 单测（reorder 校验/排序/复位）；E1 路由单测
（project 过滤组合）。
**集成层**：真实 E2E——`POST /reorder` 后 `/board` 顺序改变；`/board?project=<id>`
只返回该项目。
**上屏层**：3099 演示实例重启后人工核对 E1 下拉、E2 统计条、E3 拖拽。

## 六、风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 拖拽实现复杂/触屏不可用 | 低 | HTML5 DnD 桌面可用即达验收；触屏退回默认顺序 |
| sortOrder 与自动认领顺序冲突 | 低 | autoclaim 用自己的加权（优先级→截止→年龄），不看 list 顺序 |
| 逾期浮顶与手动顺序打架 | 低 | 手动顺序是存储事实，逾期浮顶是渲染层提示，稳定排序叠加 |
| reorder 批量写放大 | 低 | 一次拖拽一列 n 个 putTask；JSON 文件小，写前守卫逐个校验 |

## 七、完成定义

1. E1/E2/E3 验收勾选；2. 验证清单通过；3. 文档（ARCHITECTURE 48-50 / README /
   CHANGELOG）；4. 版本 `1.4.0` 提交 + tag。
