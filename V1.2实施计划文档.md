# dsh-taskboard V1.2 实施计划

> 状态：待实施 · 基线 `v1.1.0` (`c2d73fb`) · 目标 `1.2.0`
> 本文是实施依据。v1.2 = 治理：板子会满、球会等人、成本会失控——这三件事必须有出口。

---

## 一、背景（第一性原理）

v1.1 补上了「执行失控能止损」。长期使用还有三个治理缺口：

1. **板子会满**。`maxTasks` 只是拒绝创建；done 任务占着名额，完成历史无限堆积，
   活跃视图被历史淹没。需要**软归档**：done 任务退出活跃视图，仍可恢复。
2. **球会等人**。`awaiting_human` / `blocked` 的任务在等一个人，但面板没有突出
   它们；逾期任务也藏在列里。状态机「球在谁手上」的下游能力——提示人——没做。
3. **上下文成本会失控**。`budgetTokens` 只限子代理**输出**；一个巨型任务（超长
   body/spec）可以把子代理的**输入**上下文撑爆。

## 二、范围

### 做

| 编号 | 工作项 | 破坏性 | 依赖 |
|---|---|---|---|
| **C1** | 完成任务软归档：`archivedAt` + 归档动作 + 活跃视图排除 | 否（加法） | — |
| **B3** | 面板突出「球到人」：awaiting_human/blocked 计数徽标 + 逾期置顶 | 否 | — |
| **B2** | 上下文侧预算：任务级 `contextBudgetTokens`，派发前估算，超预算拒派 | 否 | — |

### 不做（附理由）

| 不做 | 理由 |
|---|---|
| 归档审计压缩（activity 裁剪恢复） | C3 单列；归档不解决 activity 裁剪，保留策略另行评估 |
| 真正的推送通知（系统通知/邮件） | dsh 当前无面向人的推送 seam；面板高亮 + 排序是插件边界内能做到的 |
| 自动归档定时器 | 归档是显式动作（用户或命令触发），不做隐式后台迁移 |

## 三、工作项详述

### C1 · 完成任务软归档

**为什么。** 活跃视图应该只显示「还欠什么」；done 是档案不是工作。

**设计。** `archivedAt: number | null`（加法字段，默认 null）：

- `TaskboardService.archiveDone(refs? )`：把指定（或全部）done 任务置
  `archivedAt = now`。显式动作，不做定时器。
- `list()` / `/board` **默认排除 archived**；`ListFilter.archived: true` 可查
- 恢复：`unarchive(ref)` 清 `archivedAt`
- 面板：done 列卡片加「归档」按钮；配置节说明（可选 `Config` 无新增）
- 导出包含 archived（备份完整性）

**验收**

- [ ] 归档后任务从默认视图消失，`archived: true` 过滤可见
- [ ] 恢复后回活跃视图
- [ ] 非 done 任务不可归档；导出含 archived

### B3 · 面板突出「球到人」

**为什么。** 等人（awaiting_human/blocked）的任务是人唯一要动的；逾期是优先级信号。

**设计（纯面板 + 无 schema）：**

- 列头计数徽标：awaiting_human/blocked 列用警示色计数
- **逾期置顶**：有 `dueAt` 且已逾期的任务在列内排序到最前（面板侧排序）
- 卡片：awaiting_human/blocked 卡片左边框加粗警示（已有 blocked 红色，awaiting_human
  补警示色）

**验收**

- [ ] 列头徽标颜色区分「等人」列
- [ ] 逾期卡片在列内置顶（渲染逻辑；上屏留给真实环境）

### B2 · 上下文侧预算

**为什么。** 输出预算限不住输入；巨型任务应被识别为不可派发。

**设计。** `contextBudgetTokens: number | null`（加法字段，默认 null = 不限制）：

- 派发时估算 prompt 成本：`estimate = ceil(promptChars / 4)`（近似 token）
- 任务有 `contextBudgetTokens` 且估算超预算 → **不派发**，直接结算
  `blocked` + 「超出上下文预算」（reason + 诊断给出估算值）
- 工具/路由透传 `context_budget_tokens`

**验收**

- [ ] 超预算任务派发时直接 blocked，子代理未启动（驱动测试：fake subagents
      starts 为空）
- [ ] 预算内任务正常派发
- [ ] 估算公式单测

## 四、实施顺序

C1 → B3 → B2。

## 五、破坏性变更与回滚

加法字段（archivedAt / contextBudgetTokens）+ 面板排序；`git reset --hard v1.1.0` 可回滚。

## 六、验证清单

**代码层**：check/build 全绿；单测覆盖归档/恢复/过滤、超预算拒派、估算公式。
**集成层**：真实 E2E——归档后任务从 /board 消失；超预算任务派发即 blocked。

## 七、风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 归档隐藏历史导致困惑 | 低 | 明确「归档 ≠ 删除」，导出含归档；恢复按钮存在 |
| 逾期置顶改变用户预期顺序 | 低 | 仅面板列内排序，不动存储顺序 |
| 字符/token 估算不准 | 低 | 只做保守上限（chars/4），宁严勿松 |

## 八、完成定义

1. C1/B3/B2 验收勾选；2. 验证清单通过；3. 文档（ARCHITECTURE 40-42 / README /
   CHANGELOG）；4. 版本 `1.2.0` 提交 + tag。
